const config = () => ({
    paths: {
        rootPathRelativeToCompiler: '../../../',
        outputBasePath: '../../../gen/',
        publicPath: '/',
        componentsPathFromRoot: '/app/components/'
    },
    PORT: 9000
});

export default config;
